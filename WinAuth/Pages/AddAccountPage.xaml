<Page
    x:Class="WinAuth.Pages.AddAccountPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:WinAuth.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:WinAuth.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:WinAuth.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodels:AddAccountViewModel}"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Button
            MinWidth="40"
            MinHeight="40"
            Margin="16,16,0,0"
            Padding="12"
            Background="Transparent"
            Canvas.ZIndex="1"
            Command="{x:Bind ViewModel.GoBackCommand}"
            Style="{StaticResource ButtonRevealStyle}">
            <FontIcon
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                FontSize="18"
                Glyph="&#xE72B;" />
        </Button>

        <controls:QRCodeScanner
            x:Name="scanner"
            Grid.RowSpan="2"
            DetectedCode="{x:Bind ViewModel.Secret, Mode=TwoWay}"
            DetectedCodeChanged="{x:Bind ViewModel.OnDetectedCodeChanged}" />
    </Grid>
</Page>